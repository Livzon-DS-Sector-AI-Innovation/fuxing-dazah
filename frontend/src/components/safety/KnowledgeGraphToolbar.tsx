'use client'

// Toolbar is now integrated into KnowledgeGraphTree.
// This file is kept for backward compatibility — no external consumers.
export default function KnowledgeGraphToolbar() {
  return null
}
