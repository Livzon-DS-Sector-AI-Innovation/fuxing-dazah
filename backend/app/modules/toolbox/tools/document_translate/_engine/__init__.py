"""ai_translator 引擎 —— GMP 文档 Word/PDF 中英双语翻译（自 ai_translator 项目 vendor）。

入口：cli.run_pipeline(Settings) 与 pdf.pipeline.run_pdf_pipeline(Settings)，
由 document_translate 工具包调用；工具箱内不可独立运行。
"""
__version__ = "0.1.0"
